# 考試智伴

名稱意涵：以考試為核心的智慧學習夥伴，兼顧複習與身心健康。

目前為 Flask 介面框架，沿用 Visual Admin 的側欄、配色與資產。
入口 `/login`，主頁 `/dashboard`，七個功能頁位於 `/workspace/<page>`。
原版型的舊功能網址已重新導向，原始模板檔保留作為版型參考，未供新介面使用。

## 啟動

安裝 requirements.txt 後執行 `python app.py`，開啟 http://127.0.0.1:5000。
目前可直接進入介面預覽，沒有帳號驗證、檔案上傳、LLM、模考評分或通知排程。
未串接的控制項已停用；畫面上的破折號表示尚無資料。

## 後續 RAG 實作邊界

- 每次檢索由後端確認使用者權限、資料庫、考試、課程及版本；不可只依靠前端選單或提示詞限制。
- 個人資料庫需綁定擁有者，系統題庫與個人資料庫分開授權；切換來源時清除或隔離舊對話與快取。
- 回答附上來源文件、章節或頁碼。檢索不足時不補寫未經支持的內容，明確提示資料不足。
- 模考從已授權題庫取題，記錄題目 ID 與版本；交卷後解析依同一題目 ID 與版本檢索，避免相似題誤配。
- 題庫缺少解析時提示尚無可用解析；作答期間不向前端傳送正確答案或解析。
- 後續考題分類、錯題統計與加強建議依實際作答紀錄與指定資料產生。

上述為待實作設計，介面本身不提供資料隔離或回答正確性保證。
